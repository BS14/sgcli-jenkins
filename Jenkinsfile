pipeline {
    agent any

    stages {
        stage('sg_cli_jobs') {
            steps {
                script {
                    def dockerImage = 'ubuntu:latest'
                    // Your other steps here
                    sh 'docker pull ubuntu:latest'
                    // Other Docker commands
                }
            }
        }
    }
}
